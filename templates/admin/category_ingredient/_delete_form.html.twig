<form method="post" action="{{ path('app_category_ingredient_delete', {'id': category_ingredient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_ingredient.id) }}">
    <button class="btn btn-danger mx-2 "><i class="fa-solid fa-trash"></i> Supprimer</button>
</form>
