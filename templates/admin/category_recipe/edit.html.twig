{% extends 'base_admin.html.twig' %}

{% block title %}Modifier une catégorie de recette{% endblock %}

{% block body %}
    <h1>Modifier une catégorie de recette</h1>

    {{ include('admin/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="d-flex flex-row">
        <a class="btn btn-secondary" href="{{ path('app_category_recipe_index') }}"><i class="fa-solid fa-rotate-left"></i> Retour</a>
        {{ include('admin/category_recipe/_delete_form.html.twig') }}
    </div>
{% endblock %}
